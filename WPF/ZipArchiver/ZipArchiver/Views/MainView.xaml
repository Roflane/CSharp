<Window x:Class="ZipArchiver.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ZipArchiver"
        xmlns:v="clr-namespace:ZipArchiver.ViewModels"
        xmlns:converters="clr-namespace:ZipArchiver.Converters"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="MainWindow" Height="450" Width="800">
    
    <Window.Resources>
        <converters:ActiveButtonConverter x:Key="ActiveButtonConverter"/>
    </Window.Resources>
    
    <Grid>
        <ContentControl Content="{Binding SelectedViewModel}"></ContentControl>
        
        <Button Width="50" Height="25" Margin="0, 0, 250, 190" Content="Zip"
                Command="{Binding UpdateViewCommand}" CommandParameter="Zip"
                Foreground="{Binding IsZipView, Converter={StaticResource ActiveButtonConverter}}"/>
        <Button Width="50" Height="25" Margin="190, 0, 0, 190" Content="Unzip"
                Command="{Binding UpdateViewCommand}" CommandParameter="Unzip"
                Foreground="{Binding IsUnzipView, Converter={StaticResource ActiveButtonConverter}}"/>
    </Grid>
</Window>
